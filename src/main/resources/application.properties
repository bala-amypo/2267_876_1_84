application properties

spring.application.name=demo

# Do NOT change the port (test case requirement)
server.port=9001

# Forward headers (for HTTPS / reverse proxy safety)
server.forward-headers-strategy=framework

spring.datasource.url=jdbc:mysql://localhost:3306/transport?
createDatabaselfNotExist=true&useSSL=false&allowPublicKeyRetrieval
=true

spring.datasource.username=root
spring.datasource.password=Amypo
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.open-in-view=false


spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

springdoc.api-docs.path=/v3/api-docs
springdoc.swagger-ui.path=/swagger-ui.html

jwt.secret=vehicleServiceHistorySecretKey
jwt.expiration=86400000